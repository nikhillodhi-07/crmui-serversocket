{
  "name": "socket-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node socketserver.js",
    "build": "echo 'No build needed'"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1",
    "cors": "^2.8.5"
  }
}
